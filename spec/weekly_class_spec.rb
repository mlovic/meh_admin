require 'rails_helper'

RSpec.describe WeeklyClass do
  #it 'returns correct time' do
    ##allow(DateTime).to receive(:now) { DateTime.new(year: 
    #now = DateTime.now
    #wc = WeeklyClass.new(starts_at: Time.parse('17:00'), ends_at: Time.parse('18:00') + 60*60, day_of_week: now.wday)
    #expect(wc.abs_start_time).to eq now.change(hour: 17, minute: 0, second: 0).to_time
  #end
end
